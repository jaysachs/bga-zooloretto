* Need to surface enclosure completions as notifications. (This can update bank money too).
  * Have Model accumulate things that can be queried later? As opposed to having to return
    everything? Things like enclosure complation, offspring growth, ...

* generally move away from %ages for flex children.

* Implement offspring for: purchase, take truck, move, exchange
  * This is pretty much done, server-side. Client-side, we still need to get
    offspring rendered before confirmation (i.e. during flow).

* Implement scoring
  * Done on backend.

* Implement money limits; where (if anywhere) to display bank money?
  * Just need to thread bank money through to client.

* Should client-side "pre-confirm" update money and show children?
  * I think it should.

* Implement enclosure completion bonuses
  * in progress

* Do we need to hide tile counts, really? if players put them in piles of 10, easy to count.
  * if we do, can we still give e.g. approx percentage left (like T&E)?
