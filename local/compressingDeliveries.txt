There are up to 6 enclosures to deliver to, and up to 3 things to deliver. We don't need to know the enclosure position, always take the first available. Placement of an animal may result in an offspring. Placement of an animal, or its generated offspring, may result in money bonus. A generated offspring may end up in the barn.

So we need to know: which truck position (1-3), and what enclosure (0-5). That's 18 possibilities. We also need to know "if offspring" (0 or 1), and money bonus (0-2), and which enclosure offspring goes to (0 for barn, 1 for current).

Actually, we can send the money bonus over with the gamedatas. So  That's 18 * 2 (offspring) * 2 (same enc or barn) = 72.

So easily 1 ASCII character (52 letters, 10 digits, plus 10 non-quote ones).

On *average* during a game, we could say there are 2 tiles per truck, and we'll do a 5p game, so 5 trucks. Let's actually say there are 3 tiles per truck.

So we have 3 bytes for each possible sequence of deliveries.

But there are 6 (3!) orderings of placements (for a full truck), and for each placement, 6 (at most, usually less) possibilities. So there are 6^4 = 1296 things. 1k basically. For 5 trucks, we're looking at 5k. That's for 5 trucks, with all 6 enclosures able to take all tiles in any order. (That'll never be the case.). Probably looking at more like 2k or so

Very possibly 4 (not 6) is the typical number. So 4^4 = 256 bytes * 5 trucks (maybe even 4). So 1k-ish.

So what does the structure of this string look like? The number of trucks is known outside. So we'll say it's

<truck1 data>:<truck2 data>:<truck3 data>:<truck4 data>
Now, if both sides know how many tiles are on each truck (they should), a given truck data is

p1.1 p2.1 p3.1 p3.2 p3.3 ...

hmm, need some "block" characters.

[ p1.1 => [ p2.1 => [ p3.1, p3.2, ..., p3.n ], p2.2 =>  [... ], ... ],
  p1.2 => ....
  ...
  p2.1 => ...
  p2.2 => ...
  ...
  p3.1 => ...
  p3.2 => ...
]

{"x":{"y":{"z","w",...},"Y":{...},...}...}

which means for each thing there are acctually 5 chars, so 5x the payload.

pos1 pos2 pos3 p1 p3 p2 p2 p1 p3 p2 p3 p1 p3 p1 ps2 p3 p2 p1

or

p1 p2 p2 p1

or

p1

For a given truck, we can just include all positions even if some are missing.

p1.1 p2.1 p3.1 p1.1 p2.1 p3.2 .... (but how many are in each? we don't know that.)

Hmm -- if we exclude offspring (which are rare!), we get 3 * 6 * 3 = 54. One byte encodes a possible placement, including sequencing. (excluding offspring).

6 sequences, each sequence has 6 * 6 * 6 possibilities. So 1400 bytes per truck.

Now, we can add in offspring, which doubles that. (Though maybe we could do the 108 possible characters). If we use the known capacities, the client can tell in enclosure or in barn, as well as completion bonus.

So it's just a string of characters, each character is a single possible sequence of placements.

Client-side, we go through all the sequences, looking for any that include each truck pos that has something as the first item.
