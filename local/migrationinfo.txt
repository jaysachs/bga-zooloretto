wagons table:
 * 3 rows
 * id / size are what you'd expect
 * val1 / val2 / val3 are the three possible tile IDs
 * status:
   'TAKEN': chosen but tile placements into enclosure not confirmed
   'AVAILABLE': not yet taken
   'PLAYED': tiles placed and confirmed

player table:
 * money: money
 * unblockedzoo: number of extensions purchased
 * skipped: "Y" means "took a wagon"
 * lastround: ???


animals table:
 * status:
    AVAILABLE in main drawpile
    LASTSET in "game end trigger" drawpile
       note: need to find out what happens when the first tile there is flipped
    WAGON in a wagon (the wagon table valN column will have it)
       x is the wagon number
       y is the position in the wagon
    THINKING placed from wagon but not confirmed
       player_id will be filled in
       x is the enclosure number
       y is the space number in the enclosure
       NOTE: it looks like stalls are part of enclosure #6, with spots labeled
          in order by enclosure number.
          So what I would consider enc1 pos 6, it has as enc6 pos1
             and enc2 pos5/6 would be enc 6 pos 2/3
    DISCARDED what happens to coins (and maybe others?)
    PLAYED same as THINKING but confirmed
    DRAWN what it says
    STALL: means fucking BARN. no position given, so they can end up moving around.
 * id is id
 * idsel -- always null?
 * idorder -- always 0?
 * player_id
 * val -- the tile type
 * x y -- location

Need to find out what happens on potential purchase and exchanges.
  answer: nothing. those are clientside only until action committed.

* As part of migration, need to undo any THINKING
  * find out which wagon is TAKEN
  * set THINKING -> WAGON
  * and put in next available wagon spot

Any THIKINGKID/THIKINGKIDSTALL just delete ?

What to do about offspring? and marking reproduced?
 * no, no record of this.
